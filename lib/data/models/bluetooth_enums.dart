/// Pairing status enum
enum PairingStatus {
  notPaired,
  pairing,
  paired,
  failed,
}

/// Connection status enum
enum ConnectionStatus {
  disconnected,
  scanning,
  connecting,
  connected,
  syncing,
}
